# OS
.DS_Store

# Design reference only — the full Tourvia Webflow export + stock footage.
# Not part of the deployed site (nothing in the site links to assets/).
assets/

# Node
node_modules/
.vercel
.env*

# Локальна заміна сховища: коли немає BLOB_READ_WRITE_TOKEN, панель пише
# вміст сюди, а завантажені файли — в images/uploads/. На сервері ні того,
# ні того не існує: там усе лежить у Vercel Blob.
.local-store/
images/uploads/
